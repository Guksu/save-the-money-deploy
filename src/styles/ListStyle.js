import styled from "styled-components";

const Li = styled.li`
  margin-top: 1%;
  font-weight: 600;
  padding-right: 50px;
`;

const RemoveBtn = styled.button``;

const ListStyle = { Li, RemoveBtn };

export default ListStyle;
